name: Welcome First-Time Contributors

on:
  pull_request:
    types: [opened]
  issues:
    types: [opened]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greet first-time contributor
        uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: |
            👋 Welcome to **Smart Attendance**!

            Thanks for opening your first issue 🎉  
            A mentor will review this soon.

            Please make sure:
            - You followed the issue template
            - You provided clear reproduction steps
            - Relevant screenshots/logs are attached

            Happy contributing 🚀

          pr-message: |
            🎉 Welcome to **Smart Attendance**!

            This is your first Pull Request — great job 🙌  
            A mentor will review it shortly.

            Before merge, ensure:
            - All CI checks pass
            - PR template is fully filled
            - Changes are scoped to the linked issue

            Thanks for contributing 🚀
